# ESTRUCTURE
src/app/
├── core/
├── shared/
├── layout/
├── features/
│   ├── auth/
│   ├── users/
│   └── movies/

----------

src/
│
├── app/
│   ├── core/
│   ├── shared/
│   ├── features/
│   ├── layout/
│   ├── app.routes.ts
│   └── app.config.ts
│
├── assets/
├── environments/
├── styles/
└── main.ts

----------

core/
├── services/
├── guards/
├── interceptors/
├── models/
└── core.module.ts (opcional en versiones nuevas)

----------

shared/
├── components/
├── directives/
├── pipes/
├── models/
└── utils/

----------

features/
├── auth/
│   ├── pages/
│   ├── components/
│   ├── services/
│   ├── auth.routes.ts
│
├── dashboard/
│   ├── pages/
│   ├── components/
│   ├── services/
│
├── users/
│   ├── pages/
│   ├── components/
│   ├── services/
│   ├── models/
│   ├── users.routes.ts

----------

layout/
├── header/
├── sidebar/
├── footer/
├── layout.component.ts

----------

assets/
├── images/
├── icons/
├── fonts/
└── i18n/

----------

environments/
├── environment.ts
├── environment.prod.ts

----------

styles/
├── base/
├── components/
├── themes/
└── styles.scss

----------

app.routes.ts




# CREATE PROJECT
ng new cine-app
cd cine-app




# CORE
ng g module core

--> Guards
ng g guard core/guards/auth --functional=false
ng g guard core/guards/role --functional=false
ng g guard core/guards/admin --functional=false
ng g guard core/guards/employee --functional=false
ng g guard core/guards/no-auth --functional=false

--> interceptors
ng g interceptor core/interceptors/auth
ng g interceptor core/interceptors/error
ng g interceptor core/interceptors/loading

--> Models
ng g interface core/models/user
ng g interface core/models/tarjet
ng g interface core/models/film
ng g interface core/models/film-ticket
ng g interface core/models/bar-ticket
ng g interface core/models/comment
ng g interface core/models/api-response

--> Services
ng g service core/services/auth
ng g service core/services/user
ng g service core/services/tarjet
ng g service core/services/film
ng g service core/services/film-ticket
ng g service core/services/bar
ng g service core/services/comment
ng g service core/services/loading
ng g service core/services/email
ng g service core/services/search




# SHARED
--> Components
ng g component shared/components/button --flat --skip-tests
ng g component shared/components/modal --flat --skip-tests
ng g component shared/components/spinner --flat --skip-tests
ng g component shared/components/alert --flat --skip-tests
ng g component shared/components/input --flat --skip-tests
ng g component shared/components/card --flat --skip-tests

--> Directives
ng g directive shared/directives/highlight
ng g directive shared/directives/loading
ng g directive shared/directives/debounce-click
ng g directive shared/directives/number-only
ng g directive shared/directives/email-validator
ng g directive shared/directives/password-match
ng g directive shared/directives/scroll-to
ng g directive shared/directives/tooltip
ng g directive shared/directives/infinite-scroll
ng g directive shared/directives/autofocus

--> Pipes
ng g pipe shared/pipes/safe-html
ng g pipe shared/pipes/truncate
ng g pipe shared/pipes/filter
ng g pipe shared/pipes/sort
ng g pipe shared/pipes/currency-format
ng g pipe shared/pipes/date-format
ng g pipe shared/pipes/age
ng g pipe shared/pipes/duration
ng g pipe shared/pipes/image-url
ng g pipe shared/pipes/phone-format
ng g pipe shared/pipes/capitalize
ng g pipe shared/pipes/stars-rating
ng g pipe shared/pipes/unique
ng g pipe shared/pipes/default-image




# LAYOUT
ng g component layout/main-layout --skip-tests
ng g component layout/header --skip-tests
ng g component layout/sidebar --skip-tests
ng g component layout/footer --skip-tests




# FEATURES
--> Auth
mkdir -p src/app/features/auth/pages/login
mkdir -p src/app/features/auth/pages/register
mkdir -p src/app/features/auth/services

ng g component features/auth/pages/login --standalone --skip-tests
ng g component features/auth/pages/register --standalone --skip-tests

ng g service features/auth/services/auth-feature --skip-tests

--> Home
mkdir -p src/app/features/home

ng g component features/home/home --standalone --skip-tests

--> Movies
mkdir -p src/app/features/movies/components/movie-card
mkdir -p src/app/features/movies/pages/movie-list
mkdir -p src/app/features/movies/pages/movie-detail
mkdir -p src/app/features/movies/services
mkdir -p src/app/features/movies/models

ng g component features/movies/components/movie-card --standalone --skip-tests
ng g component features/movies/pages/movie-list --standalone --skip-tests
ng g component features/movies/pages/movie-detail --standalone --skip-tests
ng g service features/movies/services/movies-feature --skip-tests

--> Bar
mkdir -p src/app/features/bar/components/product-card
mkdir -p src/app/features/bar/pages/bar-list
mkdir -p src/app/features/bar/pages/checkout
mkdir -p src/app/features/bar/services
mkdir -p src/app/features/bar/models

ng g component features/bar/components/product-card --standalone --skip-tests
ng g component features/bar/pages/bar-list --standalone --skip-tests
ng g component features/bar/pages/checkout --standalone --skip-tests
ng g service features/bar/services/bar-feature --skip-tests

--> Comments
mkdir -p src/app/features/comments/components/comment-item
mkdir -p src/app/features/comments/pages/comment-list
mkdir -p src/app/features/comments/pages/comment-form
mkdir -p src/app/features/comments/services

ng g component features/comments/components/comment-item --standalone --skip-tests
ng g component features/comments/pages/comment-list --standalone --skip-tests
ng g component features/comments/pages/comment-form --standalone --skip-tests
ng g service features/comments/services/comments-feature --skip-tests

--> Admin
mkdir -p src/app/features/admin/pages/admin-movies
mkdir -p src/app/features/admin/pages/admin-products
mkdir -p src/app/features/admin/pages/admin-users
mkdir -p src/app/features/admin/pages/admin-create-user
mkdir -p src/app/features/admin/services

ng g component features/admin/pages/admin-movies --standalone --skip-tests
ng g component features/admin/pages/admin-products --standalone --skip-tests
ng g component features/admin/pages/admin-users --standalone --skip-tests
ng g component features/admin/pages/admin-create-user --standalone --skip-tests
ng g service features/admin/services/admin-feature --skip-tests

--> Settings
mkdir -p src/app/features/settings

ng g component features/settings/settings --standalone --skip-tests




# ASSETS
mkdir -p src/assets/images
mkdir -p src/assets/icons
mkdir -p src/assets/fonts




# STYLES
mkdir -p src/styles/base
mkdir -p src/styles/components
mkdir -p src/styles/themes
touch src/styles/_variables.scss
touch src/styles/_mixins.scss




# ENVIRONMENTS
ng generate environments




# BONUS
ng add @angular-eslint/schematics
ng add @ngrx/store
ng add @ngrx/effects
ng add @ngrx/store-devtools